---
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="footer-inner">
    <div class="footer-top">
      <p class="footer-name"><em>mabel*</em></p>
      <div class="footer-links">
        <a href="https://github.com" target="_blank" rel="noopener">GitHub</a>
        <span class="footer-dot" aria-hidden="true">·</span>
        <a href="mailto:hello@mabel.dev">hello@mabel.dev</a>
      </div>
    </div>
    <div class="footer-bottom">
      <span>© {year}</span>
      <span class="footer-credit">Built with <a href="https://astro.build" target="_blank" rel="noopener">Astro</a></span>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--border-soft);
    margin-top: 6rem;
    padding: 2.5rem 0;
  }

  .footer-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 2rem;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .footer-top {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .footer-name {
    font-family: var(--font-display);
    font-size: 1.2rem;
    font-weight: 300;
    color: var(--ink-2);
    font-style: normal;
    margin: 0;
  }

  .footer-name em {
    font-style: italic;
  }

  .footer-links {
    display: flex;
    align-items: center;
    gap: 0.65rem;
    font-family: var(--font-ui);
    font-size: 0.78rem;
    color: var(--ink-2);
  }

  .footer-links a {
    color: var(--ink-2);
    transition: color 0.2s ease;
  }

  .footer-links a:hover { color: var(--rose); }

  .footer-dot { color: var(--ink-3); }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 1rem;
    border-top: 1px solid var(--border-soft);
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.04em;
    color: var(--ink-3);
  }

  .footer-credit a {
    color: var(--ink-3);
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .footer-credit a:hover { color: var(--rose); }
</style>
